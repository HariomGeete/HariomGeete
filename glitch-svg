<svg width="100%" height="140" viewBox="0 0 1200 140"
  xmlns="http://www.w3.org/2000/svg">

  <defs>
    <style>
      @keyframes glitch {
        0%   { transform: translate(0); opacity: 1; }
        10%  { transform: translate(-2px, 2px); }
        20%  { transform: translate(2px, -2px); }
        30%  { transform: translate(-3px, 1px); }
        40%  { transform: translate(3px, -1px); }
        50%  { transform: translate(0); }
        60%  { transform: translate(1px, -1px); }
        70%  { transform: translate(-1px, 1px); }
        80%  { transform: translate(2px, 0); }
        90%  { transform: translate(-2px, 0); }
        100% { transform: translate(0); }
      }

      @keyframes flicker {
        0%, 100% { opacity: 1; }
        50% { opacity: 0.85; }
        60% { opacity: 0.6; }
      }

      .base {
        font-family: 'Courier New', monospace;
        font-size: 56px;
        fill: #00ff9d;
        letter-spacing: 6px;
        animation: flicker 3s infinite;
      }

      .red {
        fill: #ff004c;
        animation: glitch 1.8s infinite;
      }

      .blue {
        fill: #00bfff;
        animation: glitch 2.3s infinite reverse;
      }
    </style>
  </defs>

  <!-- background -->
  <rect width="100%" height="100%" fill="#000"/>

  <!-- glitch layers -->
  <text x="50%" y="60%" text-anchor="middle" class="base red">
    YOUR NAME
  </text>

  <text x="50%" y="60%" text-anchor="middle" class="base blue">
    YOUR NAME
  </text>

  <text x="50%" y="60%" text-anchor="middle" class="base">
    YOUR NAME
  </text>

</svg>
